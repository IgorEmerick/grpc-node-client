export type TRpcRequest = {
  requestName: string;
  requestInteger: number;
  requestBoolean: boolean;
  requestFloating: number;
};

export type TRpcResponse = {
  responseName: string;
  responseIntegrer: number;
  responseBoolean: boolean;
  responseFloating: number;
};
